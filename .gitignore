# Node modules (全局忽略)
**/node_modules/

# Build / Distribution folders
**/dist/
**/build/

# pnpm / Turbo / local cache
.turbo/
.pnpm-debug.log
pnpm-lock.yaml

# macOS system files
.DS_Store

# Log files
*.log

# 环境变量 - 明确排除私密文件
.env.secrets

# Terraform state and cache
**/.terraform/
**/*.tfstate*
infra/terraform.tfvars  # 如 tfvars 有敏感变量建议也忽略

# Vitest snapshot / coverage (如启用)
coverage/
*.snap

# 编辑器文件（可选）
.vscode/
.idea/
*.swp
